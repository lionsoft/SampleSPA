'use strict';

module App {
    app.config(() => {
    Routes.push(
        {
            name: 'login',
            title: 'MENU.SignIn | Sign In',
            url: '/login'
        }
    );
    });
   
}